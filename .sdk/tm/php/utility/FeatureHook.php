<?php
declare(strict_types=1);

// Film SDK utility: feature_hook

class FilmFeatureHook
{
    public static function call(FilmContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
